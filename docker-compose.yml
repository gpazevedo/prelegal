services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - SECRET_KEY=${SECRET_KEY:-dev-secret-change-in-production}
      - DATABASE_URL=/app/data/prelegal.db
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
